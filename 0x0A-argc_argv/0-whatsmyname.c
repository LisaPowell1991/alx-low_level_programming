#include <stdio.h>
#include "main.h"

/**
 * main -  function that prints name, followed by a new line.
 * @s:char
 * Return: returns void.
 */

int main(int argc, char *argv[]) {
	printf("%s\n",argv[0]);
	return 0;
}
