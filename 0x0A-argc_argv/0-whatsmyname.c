#include <stdio.h>
#include "main.h"

/**
 * main -  function that prints name, followed by a new line.
 * @argc:integer
 * @argv:integer
 * Return: returns 0.
 */

int main(int argc, char **argv)
{
	printf("%s", argv[0]);
	return (0);
}
